# 🖖 Vue Functions

Utility functions for Vue.js development.

## `vbase`

Creates a new Vue SFC file with a basic `<script setup>`, `<template>`, and `<style scoped>` boilerplate.

### Usage
```bash
vbase <path/to/file.vue>
```
